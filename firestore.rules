rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This is an open rule for testing.
    // It allows any authenticated user to read and write all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
